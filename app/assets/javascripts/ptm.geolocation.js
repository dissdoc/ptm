(function($) {
    $.fn.ptmGeolocation = function(options) {
        var defaults = {
            width: '320px',
            height_map: '240px'
        }, 	opts = $.extend(defaults, options);

        // get value ====> opts['location']
    };
})(jQuery);