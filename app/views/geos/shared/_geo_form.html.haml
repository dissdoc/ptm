%div#geo_form{ :style => 'display:none' }
  = form_tag url, :method => :post, :remote => true do
    = geolocation(photo)
    - if user == 'member'
      = text_area_tag :comment, params[:comment], :class => 'text_lite'
    %br
    = submit_tag 'Add', :class => 'action'
    = button_tag 'Cancel', :class => 'action', :id => 'cancel_geo'