$('#comments').html("<%= escape_javascript(render(:partial => 'notes/shared/note', :collection => @photo.notes, :locals => { :photo => @photo })) %>");
$('#notes_statistic').html("<%= @photo.notes.count %>");
$('#add_comment').html("<%= escape_javascript(note_form(@photo.notes.new, :photo => @photo)) %>")