= form_tag users_path, :method => :get do
  = text_field_tag :query, params[:query]
  = submit_tag "Search", :name => nil


- if @users.present?
  - @users.each do |user|
    %div
      = user.full_name
- else
  %h3 Nothing