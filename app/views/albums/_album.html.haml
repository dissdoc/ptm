%h4= album.name

%p= album.description

= link_to "detail", album
- if signed_in? && current_user.id == album.user_id
  |
  = link_to "delete", album, :method => :delete
  |
  = link_to "sharing", album_share_path(album)