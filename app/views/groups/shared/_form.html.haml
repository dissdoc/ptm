= form_for @group do |f|
  %div
    = f.label :name
    = f.text_field :name

  %div
    = f.label :description
    = f.text_area :description

  %div
    - button_title = @group.new_record? ? "Create" : "Update"
    = f.submit button_title