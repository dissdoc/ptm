%h3= @assortment.name

%div= @assortment.description

%p
  = link_to 'Add some photos', assortment_add_photo_show_path(@assortment)

- if @assortment.photos.present?
  = render :partial => 'photos/shared/photo', :collection => @assortment.photos
- else
  Not yet!